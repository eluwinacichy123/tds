local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local screenGui = Instance.new("ScreenGui")
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

local count = 0

-- Frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 250, 0, 150)
frame.Position = UDim2.new(0.5, -125, 0.5, -75)
frame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true -- Makes the frame movable
frame.Parent = screenGui

-- Title
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 30)
title.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
title.TextColor3 = Color3.new(1, 1, 1)
title.Text = "how many gems you made"
title.Parent = frame

-- Counter Label
local label = Instance.new("TextLabel")
label.Size = UDim2.new(1, -20, 0, 50)
label.Position = UDim2.new(0, 10, 0, 40)
label.BackgroundTransparency = 1
label.TextColor3 = Color3.new(1, 1, 1)
label.TextScaled = true
label.Text = "Gems: 0"
label.Parent = frame

local amountLabel1 = game:GetService("Players").LocalPlayer.PlayerGui.ReactGameTopGameDisplay.Frame.waveTimer.container.value

local currentWave = 0
local maxWave = 0

local function updateWave()
    local current, max = amountLabel1.Text:match("(%d+)%s*/%s*(%d+)")

    wave = tonumber(current) or 0
    maxWave = tonumber(max) or 0


end

-- Initial value
updateWave()

-- Update whenever the text changes
amountLabel1:GetPropertyChangedSignal("Text"):Connect(updateWave)
print(67)
    local function fireButton(button)
        if typeof(button) ~= "Instance" then return end

        if getconnections then
            local events = {"MouseButton1Click", "MouseButton1Down", "Activated"}
            for _, eventName in ipairs(events) do
                local success, connections = pcall(getconnections, button[eventName])
                if success and connections then
                    for _, conn in ipairs(connections) do
                        pcall(conn.Fire, conn)
                    end
                end
            end
        end

        local vim = game:GetService("VirtualInputManager")
        local pos = button.AbsolutePosition + (button.AbsoluteSize / 2)
        vim:SendMouseButtonEvent(pos.X, pos.Y, 0, true,  game, 0)
        vim:SendMouseButtonEvent(pos.X, pos.Y, 0, false, game, 0)
    end

    local function parseRobux(text)
        return tonumber((text:gsub("[^%d%.]", "")))
    end
spawn(function()
while true do
local Vim = game:GetService("VirtualInputManager")

-- Press Space
Vim:SendKeyEvent(true, Enum.KeyCode.Space, false, game)

task.wait(0.1)

-- Release Space
Vim:SendKeyEvent(false, Enum.KeyCode.Space, false, game)
wait(200)
end
end)

spawn(function()
while true do 
if workspace:FindFirstChild("1") then
print('67')
local Event = game:GetService("ReplicatedStorage").RemoteFunction
Event:InvokeServer(
    "Multiplayer",
    "v2:start",
    {
        difficulty = "Easy",
        mode = "hardcore",
        count = 1
    }
)
else
print('neckhurt')
local Event = game:GetService("ReplicatedStorage").RemoteFunction
Event:InvokeServer(
    "TicketsManager",
    "UnlockTimeScale"
)
wait(0.1)


local Event = game:GetService("ReplicatedStorage").RemoteEvent
Event:FireServer(
    "TicketsManager",
    "CycleTimeScale"
)
wait(0.1)
-- This code was generated by Cobalt
-- https://github.com/notpoiu/cobalt

local Event = game:GetService("ReplicatedStorage").RemoteEvent
Event:FireServer(
    "TicketsManager",
    "CycleTimeScale"
)
wait(0.1)


local amountLabel = game:GetService("Players").LocalPlayer.PlayerGui.ReactUniversalHotbar.Frame.values.cash.amount

local cash = 0

local function updateCash()
    local cleaned = amountLabel.Text:gsub("%$", "")
    cleaned = cleaned:gsub(",", "")
    
    cash = tonumber(cleaned) or 0
    print("Cash:", cash)
end

-- Initial value
updateCash()

-- Update whenever the text changes
amountLabel:GetPropertyChangedSignal("Text"):Connect(updateCash)
repeat 
wait(0.1)
until cash >= 1850
local Event = game:GetService("ReplicatedStorage").RemoteFunction
Event:InvokeServer(
    "Troops",
    "Place",
    {
        Rotation = CFrame.new(0, 0, 0, 1, -0, 0, 0, 1, -0, 0, 0, 1),
        Position = Vector3.new(6.7438259124756, 1.1526726484299, 20.16040802002)
    },
    "Minigunner"
)
repeat 
wait(0.1)
until cash >= 475
local Event = game:GetService("ReplicatedStorage").RemoteFunction
Event:InvokeServer(
    "Troops",
    "Upgrade",
    "Set",
    {
        Troop = workspace.Towers.Default
    }
)
local Event = game:GetService("ReplicatedStorage").RemoteFunction
Event:InvokeServer(
    "Troops",
    "Place",
    {
        Rotation = CFrame.new(0, 0, 0, 1, -0, 0, 0, 1, -0, 0, 0, 1),
        Position = Vector3.new(22.7648525, 1.76591313, 11.167987)
    },
    "Scout"
)   
repeat 
wait(0.1)
until cash >= 1500
local Event = game:GetService("ReplicatedStorage").RemoteFunction
Event:InvokeServer(
    "Troops",
    "Upgrade",
    "Set",
    {
        Troop = workspace.Towers.Default
    }
)
repeat 
wait(0.1)
until cash >= 6500
local Event = game:GetService("ReplicatedStorage").RemoteFunction
Event:InvokeServer(
    "Troops",
    "Upgrade",
    "Set",
    {
        Troop = workspace.Towers.Default
    }
)
repeat
wait(0.1)
until game:GetService("Players").LocalPlayer.PlayerGui.ReactGameNewRewards.Frame.gameOver.Visible == true
print('GAME OVER')
wait(3)
if wave == 16 then
	gems += 47
elseif wave == 17 then
	gems += 51
end

label.Text = "Gems: " .. gems

local Event = game:GetService("ReplicatedStorage").RemoteFunction
Event:InvokeServer("Voting", "Skip")
print("trying to skip")
wait(5)
end
end
end)

local amountLabel2 = game:GetService("Players").LocalPlayer.PlayerGui.ReactGameTopGameDisplay.Frame.waveTimer.container.value

local time = 0

local function updateWave()
    local minutes, seconds = amountLabel2.Text:match("^(%d+):(%d+)$")

    if minutes and seconds then
        time = tonumber(minutes) * 60 + tonumber(seconds)
    else
        time = 0
    end

end

-- Initial value
updateWave()

-- Update whenever the text changes
amountLabel2:GetPropertyChangedSignal("Text"):Connect(updateWave)   
while true do
    if (wave <= 3 and time <= 10)
        or (wave > 7 and time <= 17)
        or (wave <= 6 and time <= 14) 
        or (wave <= 16 and time <= 34) then

        task.wait(0.6)

        pcall(function()
            if game:GetService("Players").LocalPlayer.PlayerGui.ReactOverridesVote.Frame.votes.Visible
                and game:GetService("Players").LocalPlayer.PlayerGui.ReactOverridesVote.Frame.Visible then

                local Event = game:GetService("ReplicatedStorage").RemoteFunction
                Event:InvokeServer("Voting", "Skip")
                print("trying to skip")
            end
        end)

    elseif amountLabel2.Text == "∞" then
        task.wait(1)

        local Event = game:GetService("ReplicatedStorage").RemoteFunction
        Event:InvokeServer("Voting", "Skip")
    end

    task.wait(0.1)
end
